package projectOne;

import org.jsfml.graphics.*;
//import org.jsfml.window.*;

public class InfoWindow {
	Sprite playerIcon = new Sprite();
	InfoWindow () {
		/*playerIcon.setPosition(Main.heightMap*64, Main.widthMap*64);
		playerIcon.setScale(128, 128);
		playerIcon.setColor(Color.BLACK);*/
	}
	InfoWindow (String palyerName, int playerID) {
		
	}
	
}
